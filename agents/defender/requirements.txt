# 방어 런타임은 Python 표준 라이브러리만 사용한다(외부 의존성 없음).
#
# 이 선택의 근거(설계 §9.3, §3 "서드파티 의존성 0개 유지"):
# - 레퍼런스 이미지가 python:3.12-slim + USER 65534 로 실행되며 설치 패키지가 없다.
# - Aho-Corasick 같은 외부 다중패턴 라이브러리는 이미지 검증 부담과 Docker 담당자
#   승인 절차를 발생시킨다. 대신 카테고리별로 결합한 단일 정규식을 미리 컴파일해
#   순회 횟수를 줄인다.
# - 이미지 시작 시간이 Round 시간에 포함되므로(운영세칙 제6조 3항) 시작 경로를
#   짧게 유지한다.
# - AI 진위 증빙(제23조) 대상 표면을 좁게 유지한다.
#
# 외부 라이브러리가 필요해지면 이 파일에 고정 버전으로 추가하고, 방어 담당자와
# 팀장 이경준의 검토를 받는다.
