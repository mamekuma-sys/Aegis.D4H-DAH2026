# 공격 런타임 설계 인수인계

> **비규범적 작업 기록:** 이 문서는 초기 설계 작업의 배경·절차·예전 점검표를 보존한다.
> 현재 공격 런타임 구현의 단일 규범적 권위는
> `docs/superpowers/specs/2026-08-11-attacker-runtime-design.md`이다. 두 문서가 다르면 해당 설계 문서와
> 저장소의 소스 우선순위를 따르고, 이 인수인계서의 구현 지시를 적용하지 않는다.

이 문서는 초기 작업 당시 공격 담당자 김태성과 AI가 함께 읽도록 작성한 실행 기록이다.
현재 구현은 위에 지정한 규범적 설계 문서를 따른다.

## 1. 이번 작업의 목표

본선 공격 에이전트의 **관측 → 계획 → 실행 → flag 제출** 구조를 설계한다. 하나의 에이전트가 본선 `FinalsPhase 1~4`에서 누적 개방되는 레이어를 증거 기반으로 식별하고, 이전 라운드에서 검증한 플레이북을 유지하면서 새 레이어에 적응하도록 설계한다. 설계는 공식 스켈레톤에서 관측 가능한 입력과 동작만 사용하며, 구현 담당자가 다음 작업 브랜치에서 테스트 주도로 코드를 작성할 수 있을 만큼 구체적이어야 한다.

이번 브랜치에서는 실제 공격 런타임 코드를 작성하지 않는다.

완료 산출물은 다음 세 파일이다.

- 새 파일: `docs/references/preliminary-report-attacker-map.md`
- 새 파일: `docs/superpowers/specs/2026-08-11-attacker-runtime-design.md`
- 수정 파일: `research/attack-scenarios.md`

`docs/references/preliminary-report-attacker-map.md`는 예선 보고서 PDF 54쪽 전체를 페이지별로 검토했다는 추적 근거다. 각 물리 PDF 페이지를 한 행씩 기록하고 본선 공격 설계에서 `계승`, `재설계`, `배경`, `제외` 중 하나로 판정한다. 각 행에는 관련 `FinalsPhase`, 본선 레이어, 예선 세그먼트를 함께 기록한다. 이 파일은 팀장 소유 경로이므로 김태성이 초안을 작성하되 팀장 이경준이 내용과 원본 동일성을 검토한다.

`research/attack-scenarios.md`에는 S1~S5별 활성화 증거, 허용된 탐색 범위, 성공 증거, 중단 조건, 관련 `FinalsPhase`와 레이어를 추가한다. 설계 본문, 페이지 매핑, 시나리오 표는 서로 모순되면 안 된다.

## 2. 운영사무국 최신 안내

2026-08-11 팀장이 전달한 운영사무국 답변을 다음과 같이 적용한다.

- 예선 제출 소스와 본선 에이전트의 구현 일관성은 요구되지 않는다.
- 예선 대비 변경점을 별도로 제출할 필요가 없다.
- 본선 코드는 공식 스켈레톤 형식과 최신 본선 규칙에 맞게 변경하거나 재구현할 수 있다.
- 이 안내는 본선의 접근 범위, rate limit, 네트워크 제한, 증빙 의무를 완화하지 않는다.

예선의 `AttackSimulationAgent`는 합성 이벤트 생성기이므로 본선 공격 런타임으로 복사하지 않는다. 예선 A1~A5와 S1~S5는 공격 가설과 계획 아이디어로만 계승한다.

본선 당일 진행 안내는 공식 운영세칙을 팀용으로 풀어쓴 파생 메모다. 운영 구조를 이해하는 입력으로 모두 읽되, 공식 운영세칙·같은 날 운영진 공지·공식 스켈레톤과 충돌하면 상위 자료를 따른다.

## 3. 소스 우선순위

사실이 충돌하면 다음 순서를 적용한다.

1. 최신 본선 규칙과 같은 날 전달된 운영사무국 지침
2. 외부 공식 스켈레톤의 `deploy/docs/agent-guide.md`
3. 공식 스켈레톤에서 직접 관측한 동작
4. 예선 보고서와 예선 소스
5. 팀 작성 문서

팀 문서와 상위 자료가 충돌하면 팀 문서를 근거로 상위 자료를 재해석하지 않는다. 충돌 내용, 근거 경로, 설계 영향만 기록하고 팀장 이경준에게 확인을 요청한다.

## 4. 작업 전 브랜치 점검

이 문서는 `docs/attacker-runtime-design` 브랜치에서 실행한다. 새 브랜치를 만들거나 `main`에서 직접 수정하지 않는다.

저장소 루트에서 다음을 실행한다.

```powershell
git status --short --branch
git fetch origin
git switch docs/attacker-runtime-design
git pull --ff-only
git branch --show-current
git merge-base --is-ancestor 6c5f8ed HEAD
```

`git branch --show-current` 출력은 `docs/attacker-runtime-design`이어야 하고 마지막 명령은 성공해야 한다. 커밋 `6c5f8ed`는 본선 자료 정렬 작업의 검토 완료 지점이다.

이 공격 설계 브랜치는 `docs/finals-source-alignment`의 정렬 커밋 위에 쌓여 있을 수 있다. 이 경우 설계 작업은 진행할 수 있지만, 공격 설계 PR을 `main`에 병합하기 전에 선행 정렬 PR이 먼저 병합됐는지 확인한다. 선행 PR이 병합된 뒤에는 `origin/main`이 현재 브랜치의 조상인지 다시 검증한다.

다음 상황에서는 삭제, reset, 강제 checkout, 강제 push를 하지 말고 중단한다.

- 작업 트리에 본인이 만들지 않은 변경이 있다.
- 현재 브랜치가 `docs/attacker-runtime-design`이 아니다.
- 정렬 커밋 `6c5f8ed`가 현재 브랜치에 포함되지 않았다.
- 브랜치에 출처를 알 수 없는 고유 커밋이 있다.
- 선행 정렬 PR보다 공격 설계 PR을 먼저 병합하려는 상태다.

## 5. 먼저 읽을 자료

다음 순서로 읽는다. 공식 스켈레톤은 이 저장소 밖에 있으며 개인 절대경로를 문서에 기록하지 않는다.

1. `AGENTS.md`
2. 같은 날 운영사무국이 전달한 최신 안내
3. 비공개 공식 원본 `DAH2026_본선운영세칙.pdf` 전체
4. 외부 공식 스켈레톤의 `deploy/docs/agent-guide.md`
5. 비공개 파생 메모 `DAH2026_본선_당일_진행_안내.md` 전체
6. `docs/references/rules-checklist.md`
7. `contracts/attacker/README.md`
8. `docs/architecture.md`
9. `docs/references/source-inventory.md`
10. `docs/references/preliminary-code-map.md`
11. 비공개 원본 `DAH2026_예선보고서_Aegis.0xD4H.pdf` 54쪽 전체
12. `agents/attacker/README.md`
13. `research/preliminary-strategy.md`
14. `research/attack-scenarios.md`
15. `docs/ownership.md`
16. `CONTRIBUTING.md`

공식 운영세칙과 당일 진행 안내의 동일성을 먼저 확인한다.

```powershell
$finalsRulesPath = Read-Host '본선 운영세칙 PDF 경로'
$finalsDayNotePath = Read-Host '본선 당일 진행 안내 Markdown 경로'
Get-FileHash -Algorithm SHA256 -LiteralPath $finalsRulesPath, $finalsDayNotePath
```

예상 SHA-256은 다음과 같다.

```text
DAH2026_본선운영세칙.pdf
FFE8E6BEECB628F93F20A9F5D0F41203CADBF28EE7E56C9A91FDAB87A1655A5F

DAH2026_본선_당일_진행_안내.md
AA704A259A3A59B10A844A59AA6B1593DEDF31D99662B70A8D7CD58EFECD5D9E
```

해시가 다르면 새 판본인지 확인하고 `docs/references/source-inventory.md`를 팀장 소유 변경으로 갱신하기 전까지 임의로 섞어 사용하지 않는다.

스켈레톤 루트가 제공됐다면 먼저 구조를 검증한다.

```powershell
pwsh -NoProfile -File scripts/validate-skeleton.ps1 -SkeletonPath <스켈레톤-루트>
```

스켈레톤 경로가 없으면 한 번만 요청한다. 공식 가이드 없이 환경변수, 제출 API 또는 런타임 동작을 추정해서 설계를 확정하지 않는다.

## 6. 예선 보고서 54쪽 전체 검토

예선 보고서는 일부 시나리오만 골라 읽지 않는다. 표지, 목차, 본문, 표, 그림, 평가, 한계, 결론을 포함한 물리 PDF 페이지 1~54를 모두 읽고 시각 요소도 확인한다. 텍스트 추출 결과만으로 그림, 표, 각주를 검토했다고 간주하지 않는다.

### 6.1 원본 동일성 확인

원본 PDF는 저장소 밖 비공개 팀 보관소에서 읽는다. 저장소 안으로 복사하거나 추출 텍스트, 렌더링 이미지, 임시 PDF를 추가하지 않는다.

```powershell
$reportPath = Read-Host '예선 보고서 PDF 경로'
Get-FileHash -Algorithm SHA256 -LiteralPath $reportPath
```

예상 SHA-256은 다음과 같다.

```text
1DD42B99A8816C3B7A543929BA0AEE90E5565CFC853E19E1455F76631CD1894E
```

해시가 다르거나 페이지 수가 54가 아니면 다른 판본일 수 있으므로 작업을 중단하고 팀장에게 보고한다.

### 6.2 페이지별 매핑 작성

`docs/references/preliminary-report-attacker-map.md`에 원본 ID `PRELIM-REPORT`, 예상 SHA-256, 확인한 물리 페이지 수 54를 기록하고 다음 표를 작성한다.

| PDF 페이지 | 인쇄 페이지 | 절·그림·표 | 핵심 내용 요약 | 예선 세그먼트 | 관련 FinalsPhase·레이어 | 공격 설계 관련성 | 판정 | 본선 근거 | 반영 위치 또는 제외 이유 |
|---:|---:|---|---|---|---|---|---|---|---|

표에는 PDF 페이지 1부터 54까지 정확히 한 행씩 넣는다. 인쇄 페이지 번호가 없으면 `없음`으로 기록한다. 여러 페이지를 한 행으로 묶거나 표지·목차·참고문헌이라는 이유로 생략하지 않는다.

판정은 다음 네 값만 사용한다.

- `계승`: 본선 설계에도 같은 원칙을 직접 사용
- `재설계`: 목적이나 아이디어는 유지하지만 본선 입력·출력·시간·네트워크 계약에 맞게 구조를 변경
- `배경`: 위협 모델, 실험 설명, 평가 맥락처럼 설계 판단의 배경으로만 사용
- `제외`: 합성 데이터 전용, 본선에서 관측 불가, 실행 권한 없음, 공격과 무관함 등의 이유로 사용하지 않음

`제외`도 누락이 아니다. 제외 이유와 이를 뒷받침하는 본선 근거를 반드시 적는다. 장문의 원문이나 표 전체를 복사하지 말고 팀의 표현으로 요약한다.

예선 세그먼트와 본선 레이어는 이름이 비슷하다는 이유만으로 같은 것으로 간주하지 않는다. 관련성을 적을 때 공식 본선 자산, 실제 서비스 응답, 공식 가이드 중 최소 하나를 근거로 제시한다.

### 6.3 빠짐없이 추출할 관점

보고서의 실제 목차와 내용을 기준으로 다음 관점을 놓치지 않는다. 보고서에 없는 항목을 만들어 채우지는 않는다.

- 문제 정의와 시스템·자산 범위
- 공격자 모델 A1~A5의 능력, 목표, 전제
- 공격 시나리오 S1~S5의 단계, 선행조건, 관측 신호, 성공 조건
- 다단계 공격에서 시간·세션·대상·인과관계가 연결되는 방식
- 이벤트 스키마와 정규화 개념
- 결정론 규칙, AI 조언, 결과 비교 구조
- 상관분석, 시간창, 인과 그래프, 위험도 계산 개념
- 대응·가용성·rollback·HITL 관련 원칙
- 데이터 생성, 평가 방법, 지표와 결과
- 예선 구현 구조와 사용 기술
- 한계, 가정, 결론, 향후 과제
- 표·그림·각주가 본문에 추가하는 제약 또는 의미

방어 중심 내용도 생략하지 않는다. 공격 런타임에 직접 사용하지 않더라도 상대 방어 모델을 이해하는 배경인지, 본선 인터페이스에서 관측 불가해 제외하는 내용인지 판정한다.

### 6.4 전체 보고서에서 설계로 연결

페이지 매핑을 먼저 완료한 뒤 공격 설계를 작성한다.

- `계승`과 `재설계`로 판정한 각 항목은 설계 문서의 정확한 절을 `반영 위치`에 기록한다.
- 설계 문서의 주요 결정은 관련 보고서 PDF 페이지와 페이지 매핑 행을 역참조한다.
- A1~A5와 S1~S5는 이름만 옮기지 말고 보고서에 적힌 전제, 단계, 성공 조건, 한계를 확인한다.
- 예선 성능 수치, 합성 탐지율, 합성 점수는 본선 예상 성능으로 사용하지 않는다.
- 보고서 내용이 본선 규칙이나 공식 스켈레톤과 충돌하면 본선 자료를 우선하고 보고서 항목은 `재설계` 또는 `제외`로 판정한다.
- 보고서에 있지만 본선에서 관측 또는 실행할 인터페이스가 증명되지 않은 기능은 코드 계획에 넣지 않는다.

54쪽 전체 검토가 끝나지 않으면 공격 런타임 설계 문서를 완료 처리하지 않는다.

## 7. 본선 FinalsPhase 1~4 적응 전략

### 7.1 서로 다른 Phase 용어 분리

문서와 향후 코드에서 다음 세 개념을 다른 이름과 타입으로 취급한다.

| 이름 | 개수 | 의미 | 사용 제한 |
|---|---:|---|---|
| `FinalsPhase` | 1~4 | 본선에서 레이어가 누적 개방되는 경기 단계 | 공식 일정 또는 실제 개방 대상 근거와 함께 사용 |
| `S4ChainStage` | 1~5 | 예선 보고서 S4의 다단계 공격 체인 국면 | `FinalsPhase` 번호와 대응한다고 가정하지 않음 |
| `MissionState` | 구현 시 enum 검토 | 이륙 전·이륙·순항·임무·복귀·착륙 같은 기체 임무 상태 | 수신 데이터 파서로 존재가 증명될 때만 사용 |

문맥 없이 `Phase 1`, `P1` 같은 표현을 사용하지 않는다. 예선 보고서의 `S4 5-Phase`는 설계 문서에서 `S4ChainStage 1~5`로 다시 표기한다.

### 7.2 공식 본선 구조와 예선 보고서 연결

본선은 4개 `FinalsPhase`, 총 14개 Round다. 새 레이어가 열려도 이전 레이어는 닫히지 않는다.

| FinalsPhase | Round 수 | 새 레이어 | 누적 개방 | 예선 보고서의 가장 가까운 세그먼트 | 기본 가설 | 활성화 전 필요한 증거 |
|---|---:|---|---|---|---|---|
| 1 | 2 | Layer 1 위성망 게이트웨이 | L1 | Communication의 일부만 참고 | 저비용 서비스·프로토콜 관측 | 실제 배너·프로토콜·응답·오류 |
| 2 | 4 | Layer 2 임무 통제 서버(MCS) | L1~L2 | Ground/GCS 중심 | S1, S4ChainStage 초기부 | 인증·세션·명령 인터페이스가 실제 노출됨 |
| 3 | 4 | Layer 3 무인이동체(UAV) | L1~L3 | Vehicle/Edge + Communication | S2, S3, 증거 기반 S4 | 파라미터·텔레메트리·상태 인터페이스가 실제 노출됨 |
| 4 | 4 | Layer 4 사족보행 로봇(UGV) | L1~L4 | Vehicle/Edge + Communication을 별도 재검증 | S2, S3, S4, 조건부 S5 | UGV 고유 인터페이스와 AI 입력 경계가 각각 실제 노출됨 |

이 표는 취약점 목록이 아니다. 위성망 게이트웨이를 예선 Communication 세그먼트와 동일시하거나, MCS를 GCS 구현과 동일시하거나, UAV의 프로토콜·파라미터를 UGV에 재사용하지 않는다. S5는 AI 방어 입력 경계가 노출됐다는 증거가 있을 때만 어느 `FinalsPhase`에서든 활성화할 수 있다.

### 7.3 하나의 누적 적응형 런타임

FinalsPhase별로 서로 다른 공격 이미지나 네 개의 독립 프로그램을 만들지 않는다. 하나의 공격 런타임이 다음 원칙으로 모든 `FinalsPhase`를 처리한다.

- 항상 `TARGETS × PORTS` 전체를 입력으로 받고 주소·포트를 하드코딩하지 않는다.
- 공식 계약에 없는 `PHASE`, `LAYER`, `ROUND` 환경변수를 요구하지 않는다.
- 표적의 배너, 응답 형태, 프로토콜 특징, 오류와 실제 성공 결과로 `ObservedServiceProfile`을 만든다.
- Phase·레이어 플레이북은 우선순위 힌트일 뿐이며, 증거가 맞지 않으면 범용 저비용 관측으로 돌아간다.
- 새 레이어가 열려도 이전 레이어의 검증된 플레이북을 중단하지 않는다.
- 전역 요청 예산을 누적 레이어 사이에 공정하게 배분하고, 성공 가능성이 확인된 표적에는 제한 안에서 우선순위를 높인다.
- 보고서 S1~S5는 `ObservedServiceProfile`과 필요한 선행 증거가 모두 맞을 때만 실행 후보가 된다.

### 7.4 FinalsPhase별 기본 라운드 목표

다음은 초기 운영 가설이다. 실제 flag 결과, 로그, PCAP, 서비스 변화가 다르면 관측 증거를 우선해 순서를 바꾼다.

| FinalsPhase | Round 1 | Round 2 | Round 3 | Round 4 |
|---|---|---|---|---|
| 1 | L1 전체 표적·서비스 inventory와 기준선 수집 | 검증된 저비용 관측·flag 추출·제출 흐름 안정화 | 해당 없음 | 해당 없음 |
| 2 | 새 MCS 서비스 fingerprint와 L1 회귀 확인 | 세션·명령 경계가 보일 때 S1 가설 검증 | L1↔L2 결과의 인과 연결과 S4ChainStage 가능성 검토 | 성공 플레이북 안정화, 실패 가설과 반복 요청 제거 |
| 3 | 새 UAV 인터페이스 식별과 L1~L2 회귀 확인 | 파라미터 인터페이스가 보일 때 S2 가설 검증 | 텔레메트리·상태 경계가 보일 때 S3 가설 검증 | 증거가 이어질 때만 L1~L3 S4 체인 검토 |
| 4 | UGV를 UAV와 다른 서비스 profile로 식별하고 L1~L3 회귀 확인 | UGV 증거에 맞춰 S2·S3 가설을 독립 재검증 | 다계층 S4와 AI 입력 경계가 보이는 경우의 S5 검토 | 가장 안정적인 고득점 플레이북 유지, 미검증 실험 축소 |

각 Round 목표에는 관측할 신호, 성공 판정, 중단 조건, 최대 요청 예산, 다음 Round로 가져갈 비밀 없는 산출물을 명시한다.

### 7.5 당일 20분 Round와 10분 Break 운영

공격 담당자의 설계에는 런타임뿐 아니라 다음 이미지로 개선하는 운영 루프도 포함한다.

```text
현재 Round 20분
  → 자동 공격 진행과 동시에 로그·PCAP·제출 결과 분석
  → 다음 버전의 플레이북·파서·우선순위 수정
  → Break 시작 시 최종 테스트와 이미지 빌드
  → Break 전반 5분 안에 Registry push 완료
  → 운영 측 pull 상태 확인
  → 다음 Round에서 새 이미지 검증
```

- 운영 측은 다음 Round 시작 5분 전에 `latest` 이미지를 가져간다.
- Break 후반까지 코드를 작성하는 계획을 세우지 않는다.
- 실행 중인 컨테이너는 새 push의 영향을 받지 않으며 다음 Round부터 반영된다.
- 공격 담당자는 변경된 실행 명령, 의존성, 테스트 결과를 Docker 담당자에게 즉시 전달한다.
- Docker 담당자가 빌드·push를 수행하고, 공격 담당자와 팀장 이경준이 공격 이미지 변경을 검토한다.
- 같은 날 운영진이 일정을 변경하면 현장 공지를 우선하고 이 기본 타임라인을 갱신한다.

### 7.6 Round 간 학습과 상태 경계

컨테이너, flag, 런타임 메모리는 Round마다 초기화된다. 과거 flag, 토큰, 세션 값, 상대 팀 비밀을 이미지나 저장소에 넣지 않는다.

다음 Round로 가져갈 수 있는 것은 팀이 검토한 비밀 없는 개발 산출물뿐이다.

- 서비스 fingerprint 규칙
- 응답 parser 개선
- 실패가 확인된 가설의 중단 규칙
- 요청 예산과 timeout 조정
- 도구 버전과 비민감 결과 코드
- 로그·PCAP 분석에서 확인한 재현 가능한 일반 패턴

Round 간 상관은 에이전트의 숨은 영속 상태가 아니라 코드·테스트·검토된 설정의 새 버전으로 구현한다.

### 7.7 FinalsPhase 적응 실패와 fallback

- 새 레이어를 식별하지 못하면 범용 저비용 관측으로 돌아가고 임의 프로토콜을 가정하지 않는다.
- 새 레이어 분석이 실패해도 이전 레이어의 검증된 플레이북과 flag 제출 흐름은 계속 동작한다.
- 로그·PCAP 제공이 늦어져도 결정론적 관측·범위 검사·제출 흐름은 중단하지 않는다.
- 이미지 build 또는 push가 pull 시각을 놓치면 다음 Round에 이전 이미지가 사용될 수 있음을 즉시 운영 담당자와 팀장에게 보고한다.
- LLM 장애나 예산 소진은 Phase 식별, rate limiting, 도구 범위 검사, flag 제출을 막지 않는다.

## 8. 고정된 본선 계약

설계에서 다음 조건을 정확히 보존한다.

### 입력과 대상

- `TARGETS`: 상대 팀 주소 CSV
- `PORTS`: 상대 팀 공통 진입 포트 CSV
- `SUBMIT_URL`: flag 제출 엔드포인트
- `SUBMIT_TOKEN`: 제출 인증 토큰
- `LLM_BASE_URL`: 대회 OpenAI 호환 LiteLLM Proxy URL
- `LLM_API_KEY`: LiteLLM 인증 키
- 공격 대상은 `TARGETS × PORTS`의 모든 조합이다.
- 주소, 포트, 토큰, 팀별 값은 코드나 문서에 하드코딩하지 않는다.

### 요청과 제출 제한

- 상대 방어망 요청: 공격 에이전트당 초당 10회, 버스트 20
- flag 제출: 모든 rolling 60초에서 최대 30회
- HTTP 429: 유효한 `Retry-After`가 Round 잔여 시간을 넘으면 현 Round에서 재시도하지 않음. header 누락·무효 시만 상한 지수 backoff
- 제출 결과: `accepted`, `own_team`, `duplicate`, `rejected`, `closed`를 구분
- 동일 flag의 불필요한 재제출을 방지

### 네트워크와 상태

- 허용된 상대 팀 진입점, 제출 서버, 대회 LiteLLM Proxy만 사용한다.
- 외부 인터넷, 운영망, 범위 밖 주소에 접근하지 않는다.
- DoS, rate limit 우회, flag 획득 목적 밖의 파괴·변조를 설계하지 않는다.
- 컨테이너와 flag는 라운드마다 초기화된다.
- 캐시, 중복 집합, 관측 이력은 라운드 한정 메모리 상태로 취급한다.
- 인증 키, 토큰, flag 원문을 로그에 남기지 않는다.

## 9. 설계 문서에 반드시 포함할 내용

`docs/superpowers/specs/2026-08-11-attacker-runtime-design.md`를 다음 구조로 작성한다. 각 절은 결정 사항, 선택 이유, 실패 시 동작을 포함해야 한다.

### 9.1 범위와 비범위

- 이번 설계가 해결하는 문제
- 공식 인터페이스 밖에서 가정하지 않는 정보
- 예선에서 계승하는 개념과 폐기하는 구현
- 실제 취약점, 데모 취약점 또는 S1~S5의 존재를 보장하지 않는다는 경계

### 9.2 근거 추적표

다음 열을 가진 표를 작성한다.

| 근거 | 예선 보고서 PDF 페이지 또는 해당 없음 | 관측·계약·보고서 사실 | 설계에 미치는 영향 |
|---|---:|---|---|

모든 환경변수, rate limit, 제출 결과, 네트워크 범위, 라운드 초기화 조건을 근거 파일과 연결한다.

`docs/references/preliminary-report-attacker-map.md`에서 `계승` 또는 `재설계`로 판정한 모든 행은 이 표나 해당 설계 절에서 PDF 페이지와 함께 역참조한다. 설계 결정이 예선 보고서의 어느 부분에도 근거하지 않는다면 본선 계약 또는 관측 결과를 근거로 명시한다.

### 9.3 공격자 모델 A1~A5

A1~A5를 각각 한 행으로 비교한다.

| 모델 | 보고서의 목표·능력·전제 | 본선에서 확인 가능한 신호 | 허용되는 설계 영향 | 사용하지 않을 가정 |
|---|---|---|---|---|

각 모델의 이름만 옮기지 말고 관련 PDF 페이지를 표시한다. 모델의 능력이나 전제가 본선 인터페이스에서 증명되지 않으면 해당 부분은 표적 우선순위나 실행 권한으로 사용하지 않는다.

### 9.4 런타임 흐름

다음 전체 흐름을 설명한다.

```text
환경변수 검증
  → TARGETS × PORTS 열거
  → 제한을 지키는 기초 관측
  → ObservedServiceProfile과 관측 증거 기록
  → 증거가 있을 때만 레이어·FinalsPhase 우선순위 힌트 선택
  → S1~S5 가설 활성화 또는 폐기
  → 허용 도구 계획 생성
  → 실행 전 범위·예산 재검증
  → 실행 결과 관측에 반영
  → flag 후보 추출·중복 제거
  → 제출·결과 기록·backoff
  → 다음 표적 또는 가설 선택
```

어떤 단계도 관측 증거 없이 다음 공격 단계로 진행하지 않는다.

### 9.5 구성요소 경계

아래 책임을 서로 분리하고, 각 구성요소의 입력·출력·의존성을 정의한다.

- 설정 검증기
- 대상 열거기와 공정한 스케줄러
- 전역 및 제출 rate limiter
- 관측 수집기, `ObservedServiceProfile` 분류기와 관측 저장소
- 누적 개방 레이어에 요청 예산을 배분하는 FinalsPhase 우선순위 정책
- S1~S5 가설 플래너
- 허용 도구 레지스트리와 실행 어댑터
- flag 후보 추출기와 중복 방지 저장소
- flag 제출 클라이언트
- LLM 조언 경계와 라운드 예산 관리자
- 비밀값을 제거하는 구조화 로그 기록기
- 전체 라운드 수명주기 오케스트레이터
- 비밀을 제거한 Round 결과 요약기

한 구성요소가 네트워크 호출, 계획 판단, 제출 상태를 동시에 소유하지 않도록 한다.

### 9.6 상태와 데이터 모델

코드를 작성하지 말고 향후 구현에 필요한 타입의 필드와 불변조건을 정의한다.

- 대상 엔드포인트
- `ObservedServiceProfile`과 근거가 포함된 선택적 `FinalsPhaseHint`
- `S4ChainStage`와, 파서로 증명된 경우에만 쓰는 `MissionState`
- 관측 결과와 증거 참조
- 시나리오 가설과 활성화·중단 이유
- 실행 계획과 요청 예산
- 도구 실행 결과
- flag 후보의 해시 기반 식별자와 제출 상태
- 라운드 예산과 LLM 사용량

본문, 응답 또는 flag 원문을 장기 로그 식별자로 사용하지 않는다. 민감하지 않은 fingerprint 또는 단방향 해시를 사용하도록 설계한다.

### 9.7 관측 전략

- 모든 `TARGETS × PORTS`를 누락 없이 다루는 방식
- 기초 관측의 종류와 최소 요청 수
- 응답 상태, 헤더, 본문 특징, 오류, 지연을 증거로 정규화하는 방식
- timeout, 연결 거부, 비정상 응답을 실패가 아닌 관측으로 기록하는 방식
- 한 표적이 전체 라운드를 독점하지 않도록 하는 공정성 정책

공식 가이드나 실제 응답으로 증명되지 않은 프로토콜 필드, 차량 상태, 파라미터 해시를 사용하지 않는다.

### 9.8 가설 플래너

S1~S5마다 다음을 정의한다.

- 활성화에 필요한 관측 증거
- 추가 관측 또는 실행의 선행조건
- 성공을 확인하는 증거
- 더 진행하지 않을 중단 조건
- 동일 실패를 반복하지 않는 재시도 정책

하드코딩된 S1 → S2 → S3 순서를 만들지 않는다. 관측 증거가 가장 강한 가설을 선택하고, 근거가 사라지거나 예산이 소진되면 폐기한다.

### 9.9 FinalsPhase 적응과 Round 운영

설계 문서에 다음을 포함한다.

- `FinalsPhase`, `S4ChainStage`, `MissionState` 용어와 타입 경계
- FinalsPhase 1~4의 새 레이어, 누적 레이어, Round 수, 예선 세그먼트 관계
- 위성망 게이트웨이·MCS·UAV·UGV별 최초 관측 증거와 fallback
- 새 레이어와 이전 레이어 사이의 공정한 요청 예산 배분
- FinalsPhase별 기본 Round 목표와 관측 증거에 따른 순서 변경 규칙
- 20분 Round 중 분석·수정하고 Break 전반 5분 안에 build·push하는 운영 루프
- Round 간 가져갈 수 있는 비밀 없는 산출물과 폐기해야 하는 상태
- 이전 이미지가 사용될 때의 감지·보고·rollback 절차

공식 계약에 없는 Phase 환경변수나 레이어 번호를 런타임 필수 입력으로 추가하지 않는다. Phase 일정과 실제 `TARGETS × PORTS`가 다르면 실행 시 관측되는 표적 집합을 우선한다.

### 9.10 실행 안전 경계

- 도구는 이름, 입력 스키마, 허용 대상, 예상 비용, timeout이 선언된 allowlist로만 실행
- 실행 직전에 대상이 `TARGETS × PORTS`에 포함되는지 재검증
- 셸 문자열 연결보다 구조화된 인자 전달을 우선
- 병렬 실행도 전역 초당 10회·버스트 20 제한을 공유
- 파괴적 동작, 지속성 확보, 범위 밖 이동, rate limit 우회를 금지
- 성공·실패·timeout을 동일한 결과 인터페이스로 관측 저장소에 반환

### 9.11 flag 생명주기

다음을 순서도 또는 상태표로 정의한다.

1. 응답에서 flag 후보 발견
2. 형식 검증
3. 원문을 로그에 남기지 않고 해시로 중복 확인
4. 공유 monotonic sliding window로 모든 rolling 60초에서 30회 이하만 제출
5. `accepted`, `own_team`, `duplicate`, `rejected`, `closed` 결과 저장
6. HTTP 429에는 유효한 `Retry-After`를 준수하고, Round 잔여 시간을 넘으면 현 Round 재시도 중단. header 누락·무효 시만 상한 지수 backoff
7. 동일 flag의 불필요한 재제출 금지

라운드 종료 시 상태가 폐기되는 점을 명시한다.

### 9.12 LLM 사용 경계

- 결정론적 파싱, 범위 검사, rate limiting, flag 제출을 LLM에 맡기지 않는다.
- LLM은 관측 요약, 가설 우선순위 조언, 다음 허용 관측 제안에만 사용한다.
- LLM 출력이 도구를 직접 실행하지 못하게 한다.
- 모든 제안은 allowlist, 대상 범위, 요청 예산 검사를 다시 통과해야 한다.
- 라운드별 호출 횟수 또는 토큰 예산과 사용 목적을 기록한다.
- 프롬프트와 로그에 `SUBMIT_TOKEN`, `LLM_API_KEY`, flag 원문을 넣지 않는다.

### 9.13 오류 처리와 종료

- 필수 환경변수 누락 또는 형식 오류
- 빈 대상 목록
- 개별 표적 timeout 또는 연결 실패
- 도구 실패
- 제출 서버의 429 및 일시 오류
- LiteLLM 장애 또는 예산 소진
- 라운드 종료 또는 프로세스 종료 신호

각 경우에 전체 런타임을 중단할지, 해당 표적만 건너뛸지, backoff 후 재시도할지 명시한다. LLM 장애는 결정론적 관측·실행·제출 흐름을 중단시키지 않도록 한다.

### 9.14 향후 파일 구조

실제 파일을 만들지 말고 다음 구현 브랜치에서 생성할 파일과 책임을 제안한다.

```text
agents/attacker/src/aegis_attacker/
├─ config.py
├─ models.py
├─ rate_limit.py
├─ observation.py
├─ profiles.py
├─ phase_policy.py
├─ planner.py
├─ tools.py
├─ flags.py
├─ llm_advisor.py
├─ audit.py
├─ round_report.py
├─ runtime.py
└─ __main__.py

agents/attacker/tests/
├─ test_config.py
├─ test_rate_limit.py
├─ test_observation.py
├─ test_profiles.py
├─ test_phase_policy.py
├─ test_planner.py
├─ test_tools.py
├─ test_flags.py
├─ test_llm_advisor.py
├─ test_round_report.py
└─ test_runtime.py
```

각 파일은 하나의 책임만 갖고, 테스트 파일은 해당 책임의 공개 동작을 검증하도록 설명한다. 구현 과정에서 더 단순한 구조가 가능하면 파일을 합칠 수 있지만 네트워크 실행, 계획 판단, flag 제출의 경계는 유지한다.

### 9.15 테스트 전략과 승인 기준

아직 테스트 코드를 작성하지 않고 다음 구현 단계에서 필요한 실패·성공 사례를 표로 정의한다.

- 환경변수 파싱과 잘못된 값 거부
- `TARGETS × PORTS` 완전 열거
- FinalsPhase 1~4 Round 수가 2·4·4·4이고 FinalsPhase N에서 Layer 1~N이 모두 스케줄됨
- `FinalsPhase`, `S4ChainStage`, `MissionState`가 서로 다른 타입과 용어로 유지됨
- 공식 계약에 없는 Phase 환경변수 없이 서비스 증거로 profile 우선순위를 선택함
- 알 수 없는 서비스가 범용 저비용 관측으로 fallback함
- 새 레이어가 열려도 이전 레이어가 요청 예산에서 굶지 않음
- UAV profile을 근거 없이 UGV에 재사용하지 않음
- 초당 10회·버스트 20 제한
- 제출 rolling 60초 최대 30회 제한, 유효한 `Retry-After` 준수, 잔여 Round 초과 시 미재시도
- 관측 증거가 없는 가설 실행 거부
- 허용 범위 밖 대상 실행 거부
- 도구 timeout과 부분 실패 격리
- flag 중복 제출 방지와 모든 제출 결과 처리
- LLM 장애·예산 소진 시 결정론적 경로 유지
- 로그에서 토큰, 키, flag 원문 제거
- Round 결과 요약에서 토큰, 키, flag, 상대 팀 비밀이 제거됨
- 라운드 종료 시 임시 상태 폐기

## 10. `research/attack-scenarios.md` 수정 방법

현재 S1~S5 표를 다음 열이 포함되도록 확장한다.

| 시나리오 | 예선 개념 | 관련 FinalsPhase·레이어 후보 | 활성화에 필요한 본선 관측 | 허용된 다음 단계 | 성공 증거 | 중단 조건 |
|---|---|---|---|---|---|---|

다음 원칙을 지킨다.

- 구체적 본선 취약점이 존재한다고 단정하지 않는다.
- 로컬 데모의 SSRF, 세션 변조, SQLi를 본선 사실로 적지 않는다.
- 활성화 근거가 없으면 해당 시나리오를 실행하지 않는다.
- FinalsPhase가 같다는 이유만으로 시나리오를 활성화하지 않는다.
- S4ChainStage 번호를 FinalsPhase 또는 레이어 번호와 대응시키지 않는다.
- 성공 증거와 중단 조건이 없는 시나리오를 남기지 않는다.
- 설계 문서의 가설 플래너 절과 동일한 용어를 사용한다.

## 11. 수정 금지 범위

이번 작업에서는 다음을 만들거나 수정하지 않는다.

- `agents/attacker/src/**`
- `agents/attacker/tests/**`
- `agents/defender/**`
- 공격·방어 Dockerfile
- `integration/**`
- CI와 이미지 스크립트
- `contracts/**`
- 공식 스켈레톤 파일

공통 계약 변경이 필요해 보이면 직접 수정하지 말고 설계 작업을 중단한 뒤 근거와 필요한 결정을 팀장에게 보고한다.

## 12. 자체 검토

설계 작성 후 다음을 확인하고 발견한 문제를 문서 안에서 바로 수정한다.

- 본선 운영세칙과 당일 진행 안내 해시가 인벤토리와 일치한다.
- 본선 구조가 4개 FinalsPhase, 14개 Round, FinalsPhase N에서 Layer 1~N 누적 개방으로 기록돼 있다.
- `FinalsPhase 1~4`, `S4ChainStage 1~5`, `MissionState`가 혼용되지 않는다.
- FinalsPhase 1~4에 각각 2·4·4·4개의 Round 목표가 있고 FinalsPhase 4에서 L1~L4가 모두 유지된다.
- FinalsPhase별 별도 이미지가 아니라 하나의 누적 적응형 런타임으로 설계돼 있다.
- 공식 계약에 없는 `PHASE`, `LAYER`, `ROUND` 환경변수를 요구하지 않는다.
- 위성망 게이트웨이·MCS·UAV·UGV 매핑은 관측 증거를 요구하며 UAV 가정을 UGV에 복사하지 않는다.
- Round 중 분석·수정하고 Break 전반 5분 안에 build·push하는 운영 루프가 있다.
- 원본 보고서 해시가 `1DD42B99A8816C3B7A543929BA0AEE90E5565CFC853E19E1455F76631CD1894E`와 일치하고 물리 페이지 수가 54다.
- 페이지 매핑에 PDF 페이지 1~54가 각각 정확히 한 번 등장한다.
- 표지, 목차, 표, 그림, 평가, 한계, 결론도 검토 대상에 포함돼 있다.
- 각 페이지에 `계승`, `재설계`, `배경`, `제외` 중 하나와 구체적인 이유가 있다.
- `계승`과 `재설계` 항목은 공격 설계의 반영 위치를 가리킨다.
- A1~A5와 S1~S5가 관련 PDF 페이지, 전제, 성공 조건, 한계와 함께 검토됐다.
- 미결정 표시, 빈 절, 임시 문구가 없다.
- 공식 자료로 증명되지 않은 입력이나 취약점을 가정하지 않는다.
- 모든 S1~S5에 활성화 증거와 중단 조건이 있다.
- 요청 및 제출 제한의 숫자가 계약과 일치한다.
- LLM이 직접 도구를 실행하거나 비밀값을 받지 않는다.
- 관측, 계획, 실행, 제출의 책임이 분리돼 있다.
- 향후 구현 파일과 테스트 책임이 일대일로 이해된다.
- 설계 문서와 `research/attack-scenarios.md`가 모순되지 않는다.

문서에 불확실한 사실을 남겨 승인받는 방식으로 완료 처리하지 않는다. 설계에 영향을 주는 사실이 확인되지 않으면 중단 조건에 따라 보고한다.

## 13. 검증 명령

저장소 루트에서 실행한다.

```powershell
pwsh -NoProfile -File scripts/check-layout.ps1
git diff --check
git diff --name-status main...HEAD
git status --short --branch
git ls-files -- '*.pdf'
rg -n "FinalsPhase|S4ChainStage|MissionState|위성망 게이트웨이|MCS|UAV|UGV|누적 개방|Break 전반 5분" docs/references/preliminary-report-attacker-map.md docs/superpowers/specs/2026-08-11-attacker-runtime-design.md research/attack-scenarios.md
```

`git ls-files -- '*.pdf'`는 출력이 없어야 한다.

`rg` 결과에는 세 Phase 용어의 정의, FinalsPhase별 레이어·Round 전략, 누적 개방, 당일 개선 루프가 모두 나타나야 한다.

페이지 매핑은 다음 검사로 1~54가 정확히 한 번씩 기록됐는지 확인한다.

```powershell
$mapPath = 'docs/references/preliminary-report-attacker-map.md'
$mappedPages = @(
    Get-Content -LiteralPath $mapPath | ForEach-Object {
        if ($_ -match '^\|\s*(\d+)\s*\|') {
            [int]$Matches[1]
        }
    }
)
$difference = Compare-Object -ReferenceObject (1..54) -DifferenceObject $mappedPages
if ($mappedPages.Count -ne 54 -or $difference) {
    throw '예선 보고서 페이지 매핑이 1~54를 정확히 한 번씩 포함하지 않습니다.'
}
Write-Output 'Preliminary report coverage check passed: 54/54 pages.'
```

스켈레톤 경로가 제공됐다면 다음도 실행한다.

```powershell
pwsh -NoProfile -File scripts/validate-skeleton.ps1 -SkeletonPath <스켈레톤-루트>
```

검증 결과에서 이번 작업의 변경 파일은 원칙적으로 다음뿐이어야 한다.

```text
docs/references/preliminary-report-attacker-map.md
docs/superpowers/specs/2026-08-11-attacker-runtime-design.md
research/attack-scenarios.md
```

기존 변경이 함께 보이면 덮어쓰거나 되돌리지 말고 본인 변경과 분리해 보고한다.

## 14. 커밋과 PR 준비

검증이 통과한 뒤 다음 커밋 메시지를 사용한다.

```text
docs: design attacker runtime
```

PR 제목 권장안은 다음과 같다.

```text
[Attacker] Design observation-plan-execution runtime
```

PR 본문에는 다음을 포함한다.

- 목표: 본선 공격 에이전트의 관측·계획·실행·제출 설계 확정
- 변경 파일과 각 파일의 역할
- 본선 운영세칙·당일 진행 안내의 SHA-256 검증 결과
- FinalsPhase 1~4의 누적 레이어·Round 전략과 세 Phase 용어 분리
- 20분 Round·10분 Break·pull 5분 전을 반영한 이미지 개선 루프
- 예선 보고서 원본 SHA-256 검증 결과와 페이지 커버리지 `54/54`
- 페이지별 `계승`·`재설계`·`배경`·`제외` 판정 요약
- 공식 계약에서 가져온 고정 조건
- A1~A5와 S1~S5에서 본선 설계로 연결한 항목
- S1~S5 활성화·중단 기준
- 실행한 검증 명령과 실제 결과
- 알려진 위험 또는 확인이 필요한 상위 자료 충돌 여부
- 다음 작업: 승인 후 `feat/attacker-runtime-foundation`에서 테스트 우선 구현

필수 검토자는 팀장 이경준이다. push, PR 생성, 병합은 사용자가 명시적으로 요청하거나 저장소 자동화에서 권한이 확인된 경우에만 수행한다. 공격 설계 PR이 승인되기 전에는 실제 공격 런타임 구현 브랜치를 시작하지 않는다.

## 15. 완료 정의

다음 조건을 모두 만족해야 완료다.

- 정확한 공격 설계 브랜치에서 작업했다.
- 본선 소스 정렬 커밋을 포함했고, 선행 정렬 PR이 병합된 뒤에는 최신 `origin/main`이 현재 브랜치의 조상인지 확인했다.
- 본선 운영세칙과 당일 진행 안내 원본의 해시를 검증하고 전체 내용을 읽었다.
- FinalsPhase 1~4의 2·4·4·4 Round와 누적 레이어 개방을 설계에 반영했다.
- `FinalsPhase`, `S4ChainStage`, `MissionState`를 구분했다.
- 하나의 누적 적응형 런타임과 당일 Round 개선 루프를 설계했다.
- 공식 `agent-guide.md`를 읽고 팀 문서와 충돌 여부를 확인했다.
- 인벤토리와 일치하는 예선 보고서 원본을 사용했다.
- 예선 보고서 물리 PDF 페이지 1~54를 모두 검토하고 페이지별 판정을 기록했다.
- `계승`과 `재설계`로 분류한 보고서 항목이 설계 문서의 반영 위치와 연결돼 있다.
- A1~A5와 S1~S5의 원래 전제·단계·성공 조건·한계를 확인했다.
- 설계 문서가 관측, 계획, 실행, flag 제출, rate limit, LLM, 오류 처리, 테스트 전략을 모두 다룬다.
- S1~S5마다 활성화 증거와 중단 조건이 있다.
- 실제 런타임 코드, Dockerfile, 공통 계약을 수정하지 않았다.
- 필수 검증 명령이 통과했다.
- 커밋과 PR 본문이 준비됐다.
- 팀장 이경준에게 검토를 요청할 수 있는 상태다.

## 16. 최종 보고 형식

작업을 마친 AI는 다음 형식으로 김태성과 팀장에게 보고한다.

```text
브랜치:
커밋:
작성·수정한 파일:
본선 운영세칙·당일 진행 안내 SHA-256 검증:
FinalsPhase·누적 레이어·Round 전략 요약:
예선 보고서 원본 SHA-256 검증:
예선 보고서 페이지 커버리지:
계승·재설계·배경·제외 건수:
확정한 핵심 설계 결정:
실행한 검증과 결과:
상위 자료 충돌 또는 차단 요소:
다음 단계:
```

차단 요소가 있으면 완료했다고 표현하지 않는다. 어떤 근거가 부족한지, 어떤 설계 결정이 영향을 받는지, 누가 무엇을 확인해야 하는지를 적는다.
