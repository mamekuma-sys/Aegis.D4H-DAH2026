# Finals 2026-08-21 P4 R11 방어 근거 매핑

원본은 private team storage에만 두며 이 저장소에는 포함하지 않는다.

| 논리 ID | 종류 | 원본 파일명 | SHA-256 |
|---|---|---|---|
| `TEAM1-PCAP-P4-R11-L4-20260821` | L4 PCAP gzip | `TEAM1-P4-R11-l4-20260821060006.pcap.gz` | `5d1a80a9fb31d1791e4fa8fece93fe708057db018ed4e49d30bfd222b449c795` |
| `TEAM1-LOG-P4-R11-DEFENDER-20260821` | defender JSONL gzip | `TEAM1-P4-R11-defender.log.gz` | `2bc8b3cd384bd76f65b50dbbc0c0cf5a91959e842737c38562e4c4131d5f4091` |

비밀값을 제거한 wire 분석에서 확인된 사실:

- `8410`·`8420` TCP에서 h2c/gRPC와 `g2dds.v1.Layer4Service`가 관측됐다.
- `ExchangeRequest` field 4는 raw protobuf, base64 protobuf, JSON protobuf를 사용했다.
- Envelope의 diagnostic, calibration, programming topic/type 조합과 정상 요청 shape를
  positive·negative fixture로 축약했다.
- HTTP/JSON 계열에는 ROS parameter 조회와 mission 명령의 민감 field/value 조합이 있었다.
- defender log의 p99 hot path는 내부 500µs 목표를 초과한 구간이 있어 port별 parser
  prefilter와 shipped-policy timing 회귀를 추가했다.

원본 payload, authorization header, token, flag, PCAP, log는 커밋하지 않는다.
