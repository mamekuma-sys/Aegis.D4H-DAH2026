# Finals 2026-08-21 P4 R13 로그 근거 매핑

원본은 private team storage에만 두며 이 저장소에는 포함하지 않는다.

| 논리 ID | 종류 | 원본 파일명 | SHA-256 |
|---|---|---|---|
| `TEAM1-LOG-P4-R13-ATTACKER-20260821` | attacker JSONL gzip | `TEAM1-P4-R13-attacker.log.gz` | `2b6806f647b4542146c98acd01517d4b3f4b55cdb3e9d02af87f5e6eaaa36b1c` |
| `TEAM1-LOG-P4-R13-DEFENDER-20260821` | defender JSONL gzip | `TEAM1-P4-R13-defender.log.gz` | `b7285b95a0a3348b173071784b9d2b011debf1f72522c61f3326a262fd5b948f` |

비밀값을 제거한 집계 분석에서 확인된 사실:

- 공격자는 제출 서버가 `accepted`로 확인한 flag 6개를 회수했다. 경로군은 MQTT 2개,
  SatDiag 2개, L2 RSC 1개, L2 GraphQL 1개이며 모두 결정론 경로였다.
- LLM 계획은 346개였지만 accepted 기여는 0개였다. 특히 `8410`·`8420`을 비롯한
  native protocol port에 HTTP GraphQL 계획이 반복돼 protocol routing 낭비가 확인됐다.
- `8410`·`8420`에서 accepted는 없었고 기존 HTTP probe도 성과가 없었다. R11 PCAP이 증명한
  G2DDS h2c/gRPC 계약을 이 두 port의 우선 전송으로 사용한다.
- defender 로그는 `0.3.6-r9-egress-reassembly`/45 rules 중 32 ACTIVE인 구버전 이미지다.
  최종 health 집계는 221,783 packet, 23,416 DROP, hot-path p99 약 0.93ms였으며 이 결과를
  현재 `0.4.0` 정책의 성능 또는 방어 성공으로 오인하지 않는다.
- 현재 정책은 위 6개 accepted 경로를 하나의 회귀 묶음으로 ACTIVE DROP 검증한다.

flag, token, authorization header, 원본 응답 body, log 원문은 커밋하지 않는다.
